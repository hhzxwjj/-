# 墨香书院书法报名管理系统——测试用例

## 一、文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 墨香书院书法报名管理系统 |
| 文档版本 | V1.0 |
| 编制日期 | 2026-05-22 |
| 测试框架 | pytest 9.0.3 |
| 用例总数 | 146 个（5 个测试文件） |

---

## 二、测试用例总览

| 测试文件 | 测试类数 | 用例数 | 测试范围 |
|----------|----------|--------|----------|
| `tests/test_auth.py` | 6 | 39 | 密码哈希、验证码、登录、注册、密码修改、资料完善 |
| `tests/test_course.py` | 3 | 17 | 课程浏览、报名、缴费 |
| `tests/test_admin.py` | 8 | 29 | 添加/删除课程、签到、人员调整、移除/添加学生、课时统计、临时调课 |
| `tests/test_message.py` | 6 | 24 | 消息中心、联系人、置顶、聊天记录、发送消息、WebSocket |
| `tests/test_security.py` | 6 | 37 | SQL 注入、XSS、权限越界、Session 安全、频率限制、错误处理 |
| **合计** | **29** | **146** | — |

---

## 三、用户认证模块测试用例

**测试文件**：`tests/test_auth.py`

### 3.1 密码哈希工具函数（TestPasswordUtils）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-PWD-01 | 哈希格式校验 | — | 调用 `hash_password('mypassword123')` | 返回 `salt$hash` 格式，salt 32 字符，hash 64 字符 | `test_hash_password_returns_salt_hash_format` |
| TC-PWD-02 | 正确密码验证 | 已生成哈希值 | 调用 `verify_password(password, hashed)` | 返回 `True` | `test_verify_password_correct` |
| TC-PWD-03 | 错误密码验证 | 已生成哈希值 | 调用 `verify_password('wrong', hashed)` | 返回 `False` | `test_verify_password_wrong` |
| TC-PWD-04 | 兼容旧版 SHA256 | 有旧版 SHA256 哈希 | 调用 `verify_password` | 旧格式密码可正确验证 | `test_verify_password_old_sha256_compatible` |
| TC-PWD-05 | 随机盐值 | — | 同一密码哈希两次 | 两次结果不同 | `test_hash_password_different_salts` |

### 3.2 验证码工具函数（TestCaptchaUtils）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-CAP-01 | 正确验证码通过 | Session 中已存储验证码 | 输入与 Session 一致的验证码 | 校验通过 | 通过登录流程间接验证 |
| TC-CAP-02 | 错误验证码拒绝 | Session 中已存储验证码 | 输入错误验证码后登录 | 返回登录页并提示验证码错误 | `test_check_captcha_wrong_via_login` |
| TC-CAP-03 | 过期验证码拒绝 | Session 中验证码已过期（>300s） | 输入过期验证码后登录 | 返回登录页并提示验证码错误 | `test_check_captcha_expired_via_login` |
| TC-CAP-04 | 空验证码拒绝 | Session 中已存储验证码 | 输入空验证码后登录 | 返回登录页并提示验证码错误 | `test_check_captcha_empty_via_login` |

### 3.3 手机号格式校验（TestPhoneValidation）

| 用例编号 | 输入手机号 | 预期结果 | pytest 参数 |
|----------|------------|----------|-------------|
| TC-PHONE-01 | `13800138000` | 合法 | `test_validate_phone[13800138000-True]` |
| TC-PHONE-02 | `13912345678` | 合法 | `test_validate_phone[13912345678-True]` |
| TC-PHONE-03 | `15012345678` | 合法 | `test_validate_phone[15012345678-True]` |
| TC-PHONE-04 | `18812345678` | 合法 | `test_validate_phone[18812345678-True]` |
| TC-PHONE-05 | `1380013800`（10位） | 非法 | `test_validate_phone[1380013800-False]` |
| TC-PHONE-06 | `138001380000`（12位） | 非法 | `test_validate_phone[138001380000-False]` |
| TC-PHONE-07 | `12800138000`（第二位2） | 非法 | `test_validate_phone[12800138000-False]` |
| TC-PHONE-08 | `1380013800a`（含字母） | 非法 | `test_validate_phone[1380013800a-False]` |
| TC-PHONE-09 | 空字符串 | 非法 | `test_validate_phone[-False]` |
| TC-PHONE-10 | `138001380 0`（含空格） | 非法 | `test_validate_phone[138001380 0-False]` |

### 3.4 登录路由（TestLoginRoute）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-LOGIN-01 | GET 登录页 | — | 访问 `/login` | 返回 200，页面渲染正常 | `test_login_page_get` |
| TC-LOGIN-02 | 正确登录 | 验证码有效 | POST 正确用户名、密码、验证码 | 登录成功，Session 写入 user_id/role | `test_login_success_with_username_password` |
| TC-LOGIN-03 | 错误密码 | 验证码有效 | POST 正确用户名、错误密码、验证码 | 返回登录页，提示"用户名或密码错误" | `test_login_wrong_password` |
| TC-LOGIN-04 | 错误验证码 | 验证码有效 | POST 正确用户名密码、错误验证码 | 返回登录页，提示"验证码错误或已过期" | `test_login_wrong_captcha` |
| TC-LOGIN-05 | 不存在用户 | 验证码有效 | POST 不存在的用户名 | 返回登录页，提示"用户名或密码错误" | `test_login_nonexistent_user` |
| TC-LOGIN-06 | 频率限制 | 验证码有效 | 连续 6 次错误登录 | 第 6 次返回 429 Too Many Requests | `test_login_rate_limit` |
| TC-LOGIN-07 | 验证码图片 | — | GET `/captcha` | 返回 PNG 图片，Content-Type=image/png | `test_captcha_image_route` |

### 3.5 注册路由（TestRegisterRoute）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-REG-01 | GET 注册页 | — | 访问 `/register` | 返回 200 | `test_register_page_get` |
| TC-REG-02 | 正常注册 | Session 中验证码有效 | POST 手机号、验证码、姓名、孩子姓名 | 注册成功，重定向到 `/register_success` | `test_register_success` |
| TC-REG-03 | 重复手机号 | Session 中验证码有效 | POST 已注册的手机号 | 返回注册页，提示"手机号已注册" | `test_register_duplicate_phone` |
| TC-REG-04 | 非法手机号 | Session 中验证码有效 | POST 格式错误的手机号 | 返回注册页，提示"手机号格式不正确" | `test_register_invalid_phone` |
| TC-REG-05 | 错误验证码 | Session 中验证码有效 | POST 错误的验证码 | 返回注册页，提示"验证码错误" | `test_register_wrong_verification_code` |

### 3.6 登出与密码修改（TestLogoutRoute / TestChangePasswordRoute）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-LOGOUT-01 | 登出清除 Session | 已登录 | GET `/logout` | 302 重定向到登录页，Session 清除 | `test_logout_clears_session` |
| TC-CPW-01 | 正确修改密码 | 已登录 | POST 正确原密码 + 新密码（≥6位） | 返回 JSON，`success=True` | `test_change_password_success` |
| TC-CPW-02 | 错误原密码 | 已登录 | POST 错误原密码 | 返回 JSON，`success=False` | `test_change_password_wrong_old` |
| TC-CPW-03 | 新密码太短 | 已登录 | POST 新密码 3 位 | 返回 JSON，`success=False` | `test_change_password_too_short` |
| TC-CPW-04 | 未登录修改密码 | 未登录 | POST 修改密码请求 | 返回 JSON，`success=False` | `test_change_password_not_logged_in` |

### 3.7 资料完善中间件（TestProfileCompleteness）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-PROF-01 | 信息不完整重定向 | 已登录，name/child_name 为"未知" | 访问非豁免路由 `/courses` | 302 重定向到 `/complete_profile` | `test_incomplete_user_redirected` |
| TC-PROF-02 | 可访问完善页面 | 信息不完整 | GET `/complete_profile` | 返回 200 | `test_complete_profile_page_accessible` |
| TC-PROF-03 | 提交完善资料 | 信息不完整 | POST 完整姓名、孩子姓名、手机号 | 更新数据库，302 重定向到首页 | `test_complete_profile_submission` |

---

## 四、课程浏览与报名缴费模块测试用例

**测试文件**：`tests/test_course.py`

### 4.1 课程浏览（TestCourseBrowse）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-COURSE-01 | 课程列表公开访问 | 未登录 | GET `/courses` | 返回 200（公开浏览） | `test_courses_page_no_login_required` |
| TC-COURSE-02 | 已登录用户浏览课程 | 已登录 | GET `/courses` | 返回 200，显示课程列表 | `test_courses_page_success` |
| TC-COURSE-03 | 课程详情需登录 | 未登录 | GET `/course/1` | 302 重定向到登录页 | `test_course_detail_requires_login` |
| TC-COURSE-04 | 查看课程详情 | 已登录 | GET `/course/1` | 返回 200，显示详情 | `test_course_detail_success` |
| TC-COURSE-05 | 不存在课程 ID | 已登录 | GET `/course/9999` | 模板报错（已知缺陷，异常被捕获） | `test_course_detail_not_found` |

### 4.2 报名路由（TestEnrollRoute）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-ENROLL-01 | 未登录报名 | 未登录 | POST `/enroll/1` | 302 重定向到登录页 | `test_enroll_requires_login` |
| TC-ENROLL-02 | 信息不完整报名 | 已登录，信息不完整 | POST `/enroll/1` | 302 重定向到 `/complete_profile` | `test_enroll_incomplete_profile` |
| TC-ENROLL-03 | 正常报名 | 已登录，信息完整 | POST 报名有余量课程 | 返回 JSON，`success=True` | `test_enroll_success` |
| TC-ENROLL-04 | 报名已满课程 | 已登录 | POST 报名已满课程 | 返回 JSON，`success=False` | `test_enroll_course_full` |
| TC-ENROLL-05 | 重复报名 | 已登录，已报名该课程 | POST 再次报名 | 返回 JSON，`success=False` | `test_enroll_duplicate` |
| TC-ENROLL-06 | 报名不存在课程 | 已登录 | POST `/enroll/9999` | 返回 JSON，`success=False` | `test_enroll_nonexistent_course` |

### 4.3 缴费路由（TestPaymentRoute）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-PAY-01 | 未登录缴费 | 未登录 | POST `/payment` | 302 重定向到登录页 | `test_payment_requires_login` |
| TC-PAY-02 | Session 无课程信息 | 已登录 | POST `/payment`（无 course_id） | 返回 JSON，`success=False` | `test_payment_no_course_in_session` |
| TC-PAY-03 | 正常缴费 | Session 中有课程信息 | POST `/payment` | 返回 JSON，`success=True`；Session 清除课程信息 | `test_payment_success` |
| TC-PAY-04 | 带备选课程缴费 | Session 中有备选课程 | POST `/payment` | 返回 JSON，`success=True` | `test_payment_with_alternate_courses` |
| TC-PAY-05 | 重复缴费 | 已报名该课程 | POST `/payment` | 返回 JSON，`success=False` | `test_payment_duplicate_enrollment` |
| TC-PAY-06 | 缴费时课程已满 | Session 中课程已满 | POST `/payment` | 返回 JSON，`success=False` | `test_payment_course_full` |

---

## 五、管理员后台模块测试用例

**测试文件**：`tests/test_admin.py`

### 5.1 权限控制（TestAdminAccessControl）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-ADMIN-01 | 未登录访问后台 | 未登录 | GET `/admin` | 302 重定向到登录页 | `test_admin_page_requires_login` |
| TC-ADMIN-02 | 普通用户访问后台 | 普通用户已登录 | GET `/admin` | 302 重定向到登录页 | `test_admin_page_requires_admin_role` |
| TC-ADMIN-03 | 管理员访问后台 | 管理员已登录 | GET `/admin` | 返回 200 | `test_admin_page_success` |

### 5.2 添加课程（TestAddCourse）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-ADDC-01 | 正常添加课程 | 管理员已登录 | POST 合法课程信息 | 添加成功，重定向到 admin | `test_add_course_success` |
| TC-ADDC-02 | 空课程名 | 管理员已登录 | POST 空 name | 重定向回 admin，未创建 | `test_add_course_empty_name` |
| TC-ADDC-03 | 负数课时 | 管理员已登录 | POST total_hours=-5 | 重定向回 admin，未创建 | `test_add_course_negative_hours` |
| TC-ADDC-04 | 负数价格 | 管理员已登录 | POST price=-100 | 重定向回 admin，未创建 | `test_add_course_negative_price` |
| TC-ADDC-05 | 0 最大学生数 | 管理员已登录 | POST max_students=0 | 重定向回 admin，未创建 | `test_add_course_zero_max_students` |
| TC-ADDC-06 | 普通用户添加课程 | 普通用户已登录 | POST 课程信息 | 302 重定向到登录页 | `test_add_course_requires_admin` |

### 5.3 删除课程（TestDeleteCourse）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-DELC-01 | 删除课程级联清理 | 管理员已登录 | POST 删除课程 | 级联删除报名、签到、备选课程 | `test_delete_course_success` |
| TC-DELC-02 | 删除不存在课程 | 管理员已登录 | POST 删除 course_id=9999 | 正常处理，无异常 | `test_delete_nonexistent_course` |
| TC-DELC-03 | 普通用户删除课程 | 普通用户已登录 | POST 删除课程 | 302 重定向到登录页 | `test_delete_course_requires_admin` |

### 5.4 签到（TestAttendance）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-ATD-01 | 普通用户访问签到页 | 普通用户已登录 | GET `/admin/attendance/1` | 302 重定向到登录页 | `test_attendance_page_requires_admin` |
| TC-ATD-02 | 管理员访问签到页 | 管理员已登录 | GET `/admin/attendance/1` | 返回 200 | `test_attendance_page_success` |
| TC-ATD-03 | 正常签到 | 管理员已登录 | POST 签到日期和出勤状态 | 创建签到记录，attendance_count 更新 | `test_attendance_post_success` |
| TC-ATD-04 | 修改已有签到 | 已有签到记录 | POST 修改出勤状态 | 更新记录，attendance_count 重新计算 | `test_attendance_post_modify_existing` |
| TC-ATD-05 | 缺席不计数 | 管理员已登录 | POST 标记 absent | attendance_count 不增加 | `test_attendance_post_absent_no_count_change` |

### 5.5 人员调整（TestMoveStudent）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-MOVE-01 | 正常调整 | 管理员已登录 | POST 学生从课程 A 调整到有余量的课程 B | 调整成功，`success=True` | `test_move_student_success` |
| TC-MOVE-02 | 调整到已满课程 | 管理员已登录 | POST 调整到已满课程 | 返回 `success=False` | `test_move_student_to_full_course` |
| TC-MOVE-03 | 学生不在当前课程 | 管理员已登录 | POST 学生不在源课程中 | 返回 `success=False` | `test_move_student_not_in_current_course` |
| TC-MOVE-04 | 学生已在目标课程 | 管理员已登录 | POST 目标课程与当前课程相同 | 返回 `success=False` | `test_move_student_already_in_target` |
| TC-MOVE-05 | 普通用户调整 | 普通用户已登录 | POST 调整请求 | 返回 `success=False` | `test_move_student_requires_admin` |

### 5.6 移除与添加学生（TestRemoveStudent / TestAddStudent）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-REMV-01 | 正常移除学生 | 管理员已登录 | POST enrollment_id | 删除报名和签到记录，课程人数减 1 | `test_remove_student_success` |
| TC-REMV-02 | 移除不存在记录 | 管理员已登录 | POST enrollment_id=9999 | 返回 `success=False` | `test_remove_student_not_found` |
| TC-REMV-03 | 普通用户移除 | 普通用户已登录 | POST 移除请求 | 返回 `success=False` | `test_remove_student_requires_admin` |
| TC-ADDS-01 | 手动添加学生 | 管理员已登录 | POST 课程 ID、学生姓名 | 创建用户和报名记录，`success=True` | `test_add_student_success` |
| TC-ADDS-02 | 缺少学生姓名 | 管理员已登录 | POST 空 child_name | 返回 `success=False` | `test_add_student_missing_child_name` |
| TC-ADDS-03 | 课程已满添加 | 管理员已登录 | POST 已满课程 | 返回 `success=False` | `test_add_student_course_full` |
| TC-ADDS-04 | 不存在课程 | 管理员已登录 | POST course_id=9999 | 返回 `success=False` | `test_add_student_nonexistent_course` |
| TC-ADDS-05 | 普通用户添加 | 普通用户已登录 | POST 添加请求 | 返回 `success=False` | `test_add_student_requires_admin` |

### 5.7 课时管理与临时调课（TestHoursManagement / TestTempAdjust）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-HOUR-01 | 普通用户访问课时管理 | 普通用户已登录 | GET `/admin/hours_management` | 302 重定向到登录页 | `test_hours_management_requires_admin` |
| TC-HOUR-02 | 管理员查看课时统计 | 管理员已登录 | GET `/admin/hours_management` | 返回 200，显示课时统计 | `test_hours_management_success` |
| TC-TEMP-01 | 正常临时调课 | 管理员已登录 | POST enrollment_id、course_id、class_date | 创建签到记录，attendance_count+1 | `test_temp_adjust_success` |
| TC-TEMP-02 | 缺少字段 | 管理员已登录 | POST 缺少必填字段 | 返回 `success=False` | `test_temp_adjust_missing_fields` |
| TC-TEMP-03 | 不存在报名记录 | 管理员已登录 | POST enrollment_id=9999 | 返回 `success=False` | `test_temp_adjust_nonexistent_enrollment` |
| TC-TEMP-04 | 普通用户调课 | 普通用户已登录 | POST 调课请求 | 返回 `success=False` | `test_temp_adjust_requires_admin` |

---

## 六、消息通信模块测试用例

**测试文件**：`tests/test_message.py`

### 6.1 消息中心与联系人（TestMessagesRoute / TestApiContacts）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-MSG-01 | 未登录访问消息中心 | 未登录 | GET `/messages` | 302 重定向到登录页 | `test_messages_requires_login` |
| TC-MSG-02 | 管理员消息中心 | 管理员已登录 | GET `/messages` | 返回 200，显示所有用户联系人 | `test_messages_admin_view` |
| TC-MSG-03 | 普通用户消息中心 | 普通用户已登录 | GET `/messages` | 返回 200，仅显示管理员联系人 | `test_messages_user_view` |
| TC-CONT-01 | 未登录获取联系人 | 未登录 | GET `/api/contacts` | 返回 401 | `test_api_contacts_requires_login` |
| TC-CONT-02 | 普通用户获取联系人 | 普通用户已登录 | GET `/api/contacts` | 返回 403 | `test_api_contacts_requires_admin` |
| TC-CONT-03 | 管理员获取联系人 | 管理员已登录 | GET `/api/contacts` | 返回 200，含置顶状态 | `test_api_contacts_admin_success` |

### 6.2 置顶与聊天记录（TestApiTogglePin / TestApiConversation）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-PIN-01 | 未登录置顶 | 未登录 | POST `/api/toggle_pin/1` | 返回 401 | `test_toggle_pin_requires_login` |
| TC-PIN-02 | 置顶联系人 | 管理员已登录 | POST 置顶用户 | 返回 `pinned=True` | `test_toggle_pin_add` |
| TC-PIN-03 | 取消置顶 | 已置顶 | POST 再次置顶同一用户 | 返回 `pinned=False` | `test_toggle_pin_remove` |
| TC-CONV-01 | 未登录获取对话 | 未登录 | GET `/api/conversation/1` | 返回 401 | `test_conversation_requires_login` |
| TC-CONV-02 | 用户查看与管理员对话 | 普通用户已登录 | GET 与管理员对话 | 返回 200，`success=True` | `test_conversation_user_to_admin` |
| TC-CONV-03 | 用户查看与其他用户对话 | 普通用户已登录 | GET 与其他用户对话 | 返回 403 | `test_conversation_user_to_other_user_forbidden` |
| TC-CONV-04 | 管理员查看与任意用户对话 | 管理员已登录 | GET 与任意用户对话 | 返回 200，`success=True` | `test_conversation_admin_to_any_user` |
| TC-CONV-05 | 未读消息标记为已读 | 有新消息 | GET 对话 | 对方发送的未读消息标记为 read | `test_conversation_marks_unread_as_read` |

### 6.3 发送消息与 WebSocket（TestSendMessage / TestWebSocketEvents）

| 用例编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|----------|-------------|
| TC-SEND-01 | 未登录发送消息 | 未登录 | POST `/send_message` | 返回 `success=False` | `test_send_message_requires_login` |
| TC-SEND-02 | 正常发送消息 | 已登录 | POST 接收人 ID 和内容 | 返回 `success=True` | `test_send_message_success` |
| TC-SEND-03 | 空消息内容 | 已登录 | POST 空内容 | 返回 `success=False` | `test_send_message_empty_content` |
| TC-SEND-04 | 消息过长 | 已登录 | POST 501 个字符 | 返回 `success=False` | `test_send_message_too_long` |
| TC-SEND-05 | 发送给不存在用户 | 已登录 | POST receiver_id=9999 | 返回 `success=False` | `test_send_message_nonexistent_receiver` |
| TC-SEND-06 | 非法接收者 ID | 已登录 | POST receiver_id='abc' | 返回 `success=False` | `test_send_message_invalid_receiver_id` |
| TC-WS-01 | 未登录 WebSocket 连接 | 未登录 | 创建 SocketIO 测试客户端 | 连接被拒绝 | `test_websocket_connect_unauthenticated` |
| TC-WS-02 | 管理员 WebSocket 连接 | 管理员已登录 | 创建 SocketIO 测试客户端 | 连接成功，收到 connected 事件 | `test_websocket_connect_admin` |
| TC-WS-03 | 加入/离开课程房间 | 已登录 | emit join_course / leave_course | 收到 joined_course / left_course | `test_websocket_join_leave_course` |
| TC-WS-04 | 发送消息到房间 | 已登录 | emit send_message | 收到 new_message 或 connected | `test_websocket_send_message` |
| TC-WS-05 | 发送空消息 | 已登录 | emit send_message（空内容） | 收到 error 事件 | `test_websocket_send_message_empty` |
| TC-WS-06 | 普通用户签到更新 | 普通用户已登录 | emit attendance_update | 收到 error 事件（权限不足） | `test_websocket_attendance_update_requires_admin` |
| TC-WS-07 | 无效房间名 | 已登录 | emit send_message（非法 room） | 收到 error 事件 | `test_websocket_invalid_room_rejected` |

---

## 七、安全测试模块测试用例

**测试文件**：`tests/test_security.py`

### 7.1 SQL 注入防护（TestSQLInjection）

| 用例编号 | 用例名称 | 测试 Payload | 预期结果 | pytest 函数 |
|----------|----------|--------------|----------|-------------|
| TC-SQL-01 | UNION 注入 | `username="' OR '1'='1"` | 登录失败，返回登录页 | `test_login_sql_injection_union` |
| TC-SQL-02 | 注释注入 | `username="admin'--"` | 登录失败，返回登录页 | `test_login_sql_injection_comment` |
| TC-SQL-03 | UNION SELECT 注入 | `username="' UNION SELECT * FROM users --"` | 登录失败，返回登录页 | `test_login_sql_injection_union_select` |
| TC-SQL-04 | 消息内容 SQL 注入 | `content="'; DROP TABLE users; --"` | 消息作为纯文本存储，表未被删除 | `test_send_message_sql_injection` |
| TC-SQL-05 | JSON 中 SQL 注入 | `alternate_courses=["1; DROP TABLE courses;"]` | 报名正常处理，表未被删除 | `test_enroll_sql_injection_in_json` |

### 7.2 XSS 防护（TestXSS）

| 用例编号 | 用例名称 | 测试 Payload | 预期结果 | pytest 函数 |
|----------|----------|--------------|----------|-------------|
| TC-XSS-01 | script 标签 | `<script>alert("XSS")</script>` | 作为纯文本存储，未被执行为脚本 | `test_send_message_xss_script` |
| TC-XSS-02 | onerror 事件 | `<img src=x onerror=alert(1)>` | 作为纯文本存储 | `test_send_message_xss_img_onerror` |
| TC-XSS-03 | javascript 伪协议 | `<a href="javascript:alert(1)">点击</a>` | 作为纯文本存储 | `test_send_message_xss_javascript_protocol` |

### 7.3 权限越界（TestPrivilegeEscalation）

| 用例编号 | 用例名称 | 测试接口 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|-------------|
| TC-PRIV-01 | 普通用户添加课程 | POST `/admin/add_course` | 302 重定向到登录页 | `test_user_access_admin_api_add_course` |
| TC-PRIV-02 | 普通用户删除课程 | POST `/admin/delete_course/1` | 302 重定向到登录页 | `test_user_access_admin_api_delete_course` |
| TC-PRIV-03 | 普通用户访问签到页 | GET `/admin/attendance/1` | 302 重定向到登录页 | `test_user_access_admin_api_attendance` |
| TC-PRIV-04 | 普通用户访问课时管理 | GET `/admin/hours_management` | 302 重定向到登录页 | `test_user_access_hours_management` |
| TC-PRIV-05 | 普通用户调整人员 | POST `/move_student` | 返回 `success=False` | `test_user_move_student_api` |
| TC-PRIV-06 | 普通用户移除学生 | POST `/remove_student` | 返回 `success=False` | `test_user_remove_student_api` |
| TC-PRIV-07 | 普通用户临时调课 | POST `/temp_adjust` | 返回 `success=False` | `test_user_temp_adjust_api` |
| TC-PRIV-08 | 普通用户添加学生 | POST `/add_student` | 返回 `success=False` | `test_user_add_student_api` |
| TC-PRIV-09 | 普通用户获取联系人 | GET `/api/contacts` | 返回 403 | `test_user_access_api_contacts` |
| TC-PRIV-10 | 普通用户查看他人对话 | GET `/api/conversation/3` | 返回 403 | `test_user_access_other_user_conversation` |

### 7.4 Session 安全（TestSessionSecurity）

| 用例编号 | 用例名称 | 预期结果 | pytest 函数 |
|----------|----------|----------|-------------|
| TC-SESS-01 | HttpOnly 标志 | `SESSION_COOKIE_HTTPONLY=True` | `test_session_http_only` |
| TC-SESS-02 | SameSite 标志 | `SESSION_COOKIE_SAMESITE='Lax'` | `test_session_samesite` |
| TC-SESS-03 | Session 过期时间 | `PERMANENT_SESSION_LIFETIME` 已配置 | `test_session_has_lifetime` |

### 7.5 频率限制（TestRateLimit）

| 用例编号 | 用例名称 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|-------------|
| TC-RATE-01 | 5 次请求后触发限制 | 同一 IP 连续 6 次请求 `/login` | 第 6 次返回 429 | `test_rate_limit_triggers_after_5_requests` |
| TC-RATE-02 | 429 响应格式 | 触发频率限制 | 返回 JSON，`success=False`，含 message | `test_rate_limit_429_response_format` |

### 7.6 错误处理（TestErrorHandlers）

| 用例编号 | 用例名称 | 测试步骤 | 预期结果 | pytest 函数 |
|----------|----------|----------|----------|-------------|
| TC-ERR-01 | API 404 返回 JSON | GET `/api/nonexistent` | 返回 404 JSON | `test_404_api_returns_json` |
| TC-ERR-02 | 页面 404 返回模板 | GET `/nonexistent-page` | 返回 404 HTML | `test_404_page_returns_template` |
| TC-ERR-03 | 500 错误处理器 | — | 500 处理器已注册 | `test_500_error_handler_exists` |
| TC-ERR-04 | 429 错误处理器 | — | 429 处理器已注册 | `test_429_error_handler_exists` |

---

## 八、测试覆盖矩阵

| 需求模块 | 功能点 | 单元测试 | 集成测试 | 系统测试 | 安全测试 | WebSocket 测试 |
|----------|--------|----------|----------|----------|----------|----------------|
| 用户认证 | 密码哈希 | ✓ | ✓ | ✓ | ✓ | — |
| 用户认证 | 验证码 | ✓ | ✓ | ✓ | — | — |
| 用户认证 | 登录/注册 | — | ✓ | ✓ | ✓ | — |
| 用户认证 | 资料完善 | — | ✓ | ✓ | — | — |
| 课程管理 | 课程浏览 | — | ✓ | ✓ | — | — |
| 课程管理 | 报名缴费 | — | ✓ | ✓ | ✓ | — |
| 管理后台 | 增删课程 | — | ✓ | ✓ | ✓ | — |
| 管理后台 | 签到/课时 | — | ✓ | ✓ | — | — |
| 管理后台 | 人员调整 | — | ✓ | ✓ | ✓ | — |
| 消息通信 | 发送/接收 | — | ✓ | ✓ | ✓ | ✓ |
| 消息通信 | 联系人/置顶 | — | ✓ | ✓ | ✓ | — |
| 全局 | 频率限制 | — | ✓ | ✓ | — | — |
| 全局 | 错误处理 | — | ✓ | ✓ | — | — |

---

## 九、pytest 自动化测试执行说明

### 9.1 运行全部测试

```bash
# 进入项目目录
cd d:/trae_projects/BaoMing

# 使用虚拟环境中的 pytest
.venv/Scripts/python.exe -m pytest tests/ -v
```

### 9.2 运行指定模块

```bash
# 仅运行认证模块
.venv/Scripts/python.exe -m pytest tests/test_auth.py -v

# 仅运行安全测试
.venv/Scripts/python.exe -m pytest tests/test_security.py -v
```

### 9.3 生成测试报告

```bash
# 生成 HTML 报告
.venv/Scripts/python.exe -m pytest tests/ --html=report.html

# 生成覆盖率报告
.venv/Scripts/python.exe -m pytest tests/ --cov=app --cov-report=html
```

### 9.4 当前测试结果

**执行时间**：2026-05-22  
**测试用例总数**：146 个  
**通过数**：146 个  
**失败数**：0 个  
**通过率**：**100%**
